interface projectConfig {
  header: {
    text: string;
  };
  footer: {
    text: string;
  };
}

const projectConfig: projectConfig = {
  header: {
    text: "NPD"
  },
  footer: {
    text: "Nasze pierwsze dziecko"
  }
};

export default projectConfig;
